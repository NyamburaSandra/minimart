# Project: Marketing & Growth Hackathon - Backend Skeleton (Django + DRF)
# Updated docker-compose.yml and Dockerfile with fixes for Gunicorn & Celery startup

### file: docker-compose.yml
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: hackdb
      POSTGRES_USER: hackuser
      POSTGRES_PASSWORD: hackpass
    ports:
      - "5434:5432"  # avoid conflict with local Postgres
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6380:6379"

  web:
    build:
      context: ./backend
    command: gunicorn marketinghack.marketinghack.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    environment:
      PYTHONPATH: /app
      DJANGO_SETTINGS_MODULE: marketinghack.settings
      DATABASE_URL: postgres://hackuser:hackpass@db:5432/hackdb
      REDIS_URL: redis://redis:6379/0

  worker:
    build:
      context: ./backend
    command: celery -A marketinghack.celery worker -l info
    volumes:
      - ./backend:/app
    depends_on:
      - redis
      - db
    environment:
      PYTHONPATH: /app
      DJANGO_SETTINGS_MODULE: marketinghack.settings
      DATABASE_URL: postgres://hackuser:hackpass@db:5432/hackdb
      REDIS_URL: redis://redis:6379/0

  beat:
    build:
      context: ./backend
    command: celery -A marketinghack.celery beat -l info
    volumes:
      - ./backend:/app
    depends_on:
      - redis
      - db
    environment:
      PYTHONPATH: /app
      DJANGO_SETTINGS_MODULE: marketinghack.settings
      DATABASE_URL: postgres://hackuser:hackpass@db:5432/hackdb
      REDIS_URL: redis://redis:6379/0

volumes:
  pgdata: